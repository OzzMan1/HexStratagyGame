extends Node


func find_collisions():
	print("shut it") 
	
	# var occupied : Dictionary[target, unit]
	# var conflict : Dictionary[target, Array]
	
	# we have all players unit orders, combine them 
	# we sort it by inititative desc
	# Split up into groups of initiative 
	# for each intiative 
		# randomise units  	 
		# then iterate through 
			# if occupied is not taken
			# update occupied 
			# else 
				# add conflict 
	# 	

			
# func add_conflict(conflict_location, (unit,player)) 
	# check if there is a conflict their 
		# add (unit,player)
	# 

			
			
		

# func resolve_conflict_peace()
	# Get incoming unit direction

# func resolve_conflict_war() 
	# 
